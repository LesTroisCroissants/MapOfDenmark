package program;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class AddressBookTest {/*
    AddressBook addressBook;
    @Test
    void addressBookTest() {
        Address addr = new Address("Rued Langgaards Vej", "7", "2300", "København S");
        MapPoint actualMapPoint = new MapPoint(42.01,69.69);
        addressBook.addAddress("Rued Langgaards Vej", "7, 2300 København S", mapPoint);
        MapPoint mapPoint = addressBook.addressSearch(addr);
        assertEquals(mapPoint, actualMapPoint);
        assertTrue(true);
    }
*/
}
